{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Window lifecycle for the GUI (loopback http content) and the pet window, which moves itself via setPosition while dragging. Remote origins: the harness serves both windows over loopback http.",
  "windows": [
    "main",
    "pet"
  ],
  "remote": {
    "urls": [
      "http://127.0.0.1:*",
      "http://localhost:*"
    ]
  },
  "permissions": [
    "core:default",
    "core:window:allow-outer-position",
    "core:window:allow-set-position"
  ]
}
